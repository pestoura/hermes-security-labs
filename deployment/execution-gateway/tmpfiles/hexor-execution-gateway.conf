# tmpfiles.d declaration for the Hexor Execution Gateway runtime directory (HOLD
# boundary). This is a repository-only declaration; the gateway is the AF_UNIX
# CLIENT and does NOT own /run/hexor. The runtime directory is owned by the runner
# runtime boundary tmpfiles.d (hexor-runner.conf: /run/hexor 0750 4101 4110). This
# file documents the gateway's expectation and is intentionally inert: it creates
# nothing, to avoid fighting the runner side for the directory ownership.
#
# Format: Type Path Mode UID GID Age Argument
# (no line: the gateway must NOT create /run/hexor; documented in README only)
