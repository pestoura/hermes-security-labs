name: Apply EPIC-05 supervisor docs once

on:
  push:
    branches:
      - feat/epic-05-supervised-process-boundary

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/epic-05-supervised-process-boundary
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Apply exact documentation patch
        run: python tools/tmp_epic05_supervisor_docs_patch.py
      - name: Install validation dependencies and SDK
        run: python -m pip install --disable-pip-version-check -q pytest ruff -e platform/runner-protocol
      - name: Validate supervised boundary and lifecycle synchronization
        run: |
          python platform/runner-protocol/validate_protocol.py --contract-only
          python -m pytest -q \
            platform/runner-protocol/tests \
            docs/tests roadmap/tests \
            -p no:cacheprovider
          ruff check --config security/pyproject.toml \
            platform/runner-protocol \
            docs/tests roadmap/tests
          git diff --check
      - name: Commit validated documentation state
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "docs(epic-05): start supervised process boundary"
          git push origin HEAD:feat/epic-05-supervised-process-boundary
