apiVersion: security.hex0r.io/v1alpha1
kind: SecurityCampaign
metadata:
  id: retest
  name: Finding retest
  status: experimental
domain: devsecops
selectors:
  finding_refs_required: true
execution:
  mode: sequential
  max_parallel: 1
  stop_on_scope_violation: true
  stop_on_cleanup_failure: true
