apiVersion: security.hex0r.io/v1alpha1
kind: SecurityRunbook
metadata:
  id: DEVSEC-SECRETS-013
  name: Verify Terraform State Secret
  version: 1.0.0
  status: experimental
  domain: devsecops
  category: secrets
  tags:
  - owasp-devsecops
  - secrets
  - terraform-state-secret
  - typed-execution
selectors:
  target_types:
  - git-repository
  capabilities:
  - repository-read
  - secret-scanner
risk:
  intrusiveness: passive
  destructive: false
  production_safe: true
  max_actions: 5
  timeout_seconds: 180
steps:
- id: collect-baseline
  provider: repository
  action: inventory
  profile: secrets-baseline
  arguments:
    target_ref: '{{ target.ref }}'
    evidence_ref: baseline/DEVSEC-SECRETS-013
    read_only: true
    redact_secrets: true
- id: execute-control
  provider: secrets
  action: scan
  profile: terraform-state-secret
  arguments:
    target_ref: '{{ target.ref }}'
    control_id: DEVSEC-SECRETS-013
    rule_profile: terraform-state-secret
    fail_closed: false
    redact_secrets: true
    collect_tool_version: true
    evidence_ref: findings/DEVSEC-SECRETS-013
- id: verify-evidence
  provider: evidence
  action: verify
  profile: deterministic-finding-bundle
  arguments:
    target_ref: '{{ target.ref }}'
    control_id: DEVSEC-SECRETS-013
    required_artifacts:
    - tool-version
    - scan-summary
    - sanitised-observations
    redact_secrets: true
evaluation:
  vulnerable_when:
  - control.terraform_state_secret.status equals non_compliant
  - control.terraform_state_secret.evidence_count is greater than zero
  - scanner.completed equals true and scanner.error_count equals zero
  secure_when:
  - control.terraform_state_secret.status equals compliant
  - control.terraform_state_secret.evidence_count equals zero
  - scanner.completed equals true and baseline.coverage_complete equals true
  inconclusive_when:
  - control.terraform_state_secret.status equals unknown
  - scanner.completed equals false or scanner.error_count is greater than zero
  - baseline.coverage_complete equals false or required evidence is missing
outputs:
  title: Terraform State Secret control result
  severity: medium
  confidence: dynamic
