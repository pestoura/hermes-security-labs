apiVersion: security.hex0r.io/v1alpha1
kind: SecurityRunbook
metadata:
  id: DEVSEC-SECRETS-008
  name: Verify Database Uri
  version: 1.0.0
  status: experimental
  domain: devsecops
  category: secrets
  tags:
  - database-uri
  - owasp-devsecops
  - secrets
  - typed-execution
selectors:
  target_types:
  - git-repository
  capabilities:
  - repository-read
  - secret-scanner
risk:
  intrusiveness: passive
  destructive: false
  production_safe: true
  max_actions: 5
  timeout_seconds: 180
steps:
- id: collect-baseline
  provider: repository
  action: inventory
  profile: secrets-baseline
  arguments:
    target_ref: '{{ target.ref }}'
    evidence_ref: baseline/DEVSEC-SECRETS-008
    read_only: true
    redact_secrets: true
- id: execute-control
  provider: secrets
  action: scan
  profile: database-uri
  arguments:
    target_ref: '{{ target.ref }}'
    control_id: DEVSEC-SECRETS-008
    rule_profile: database-uri
    fail_closed: false
    redact_secrets: true
    collect_tool_version: true
    evidence_ref: findings/DEVSEC-SECRETS-008
- id: verify-evidence
  provider: evidence
  action: verify
  profile: deterministic-finding-bundle
  arguments:
    target_ref: '{{ target.ref }}'
    control_id: DEVSEC-SECRETS-008
    required_artifacts:
    - tool-version
    - scan-summary
    - sanitised-observations
    redact_secrets: true
evaluation:
  vulnerable_when:
  - control.database_uri.status equals non_compliant
  - control.database_uri.evidence_count is greater than zero
  - scanner.completed equals true and scanner.error_count equals zero
  secure_when:
  - control.database_uri.status equals compliant
  - control.database_uri.evidence_count equals zero
  - scanner.completed equals true and baseline.coverage_complete equals true
  inconclusive_when:
  - control.database_uri.status equals unknown
  - scanner.completed equals false or scanner.error_count is greater than zero
  - baseline.coverage_complete equals false or required evidence is missing
outputs:
  title: Database Uri control result
  severity: high
  confidence: dynamic
