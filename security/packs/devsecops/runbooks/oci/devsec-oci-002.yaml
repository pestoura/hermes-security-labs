apiVersion: security.hex0r.io/v1alpha1
kind: SecurityRunbook
metadata:
  id: DEVSEC-OCI-002
  name: Verify Privileged Container
  version: 1.0.0
  status: experimental
  domain: devsecops
  category: oci
  tags:
  - oci
  - owasp-devsecops
  - privileged-container
  - typed-execution
selectors:
  target_types:
  - oci-image
  capabilities:
  - registry-read
  - image-metadata-read
risk:
  intrusiveness: passive
  destructive: false
  production_safe: true
  max_actions: 5
  timeout_seconds: 300
steps:
- id: collect-baseline
  provider: oci
  action: inspect
  profile: oci-baseline
  arguments:
    target_ref: '{{ target.ref }}'
    evidence_ref: baseline/DEVSEC-OCI-002
    read_only: true
    redact_secrets: true
- id: execute-control
  provider: oci
  action: inspect
  profile: privileged-container
  arguments:
    target_ref: '{{ target.ref }}'
    control_id: DEVSEC-OCI-002
    rule_profile: privileged-container
    fail_closed: false
    redact_secrets: true
    collect_tool_version: true
    evidence_ref: findings/DEVSEC-OCI-002
- id: verify-evidence
  provider: evidence
  action: verify
  profile: deterministic-finding-bundle
  arguments:
    target_ref: '{{ target.ref }}'
    control_id: DEVSEC-OCI-002
    required_artifacts:
    - tool-version
    - scan-summary
    - sanitised-observations
    redact_secrets: true
evaluation:
  vulnerable_when:
  - control.privileged_container.status equals non_compliant
  - control.privileged_container.evidence_count is greater than zero
  - scanner.completed equals true and scanner.error_count equals zero
  secure_when:
  - control.privileged_container.status equals compliant
  - control.privileged_container.evidence_count equals zero
  - scanner.completed equals true and baseline.coverage_complete equals true
  inconclusive_when:
  - control.privileged_container.status equals unknown
  - scanner.completed equals false or scanner.error_count is greater than zero
  - baseline.coverage_complete equals false or required evidence is missing
outputs:
  title: Privileged Container control result
  severity: high
  confidence: dynamic
