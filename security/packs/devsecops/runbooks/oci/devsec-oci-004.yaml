apiVersion: security.hex0r.io/v1alpha1
kind: SecurityRunbook
metadata:
  id: DEVSEC-OCI-004
  name: Verify Writable Root Filesystem
  version: 1.0.0
  status: experimental
  domain: devsecops
  category: oci
  tags:
  - oci
  - owasp-devsecops
  - typed-execution
  - writable-root-filesystem
selectors:
  target_types:
  - oci-image
  capabilities:
  - registry-read
  - image-metadata-read
risk:
  intrusiveness: passive
  destructive: false
  production_safe: true
  max_actions: 5
  timeout_seconds: 300
steps:
- id: collect-baseline
  provider: oci
  action: inspect
  profile: oci-baseline
  arguments:
    target_ref: '{{ target.ref }}'
    evidence_ref: baseline/DEVSEC-OCI-004
    read_only: true
    redact_secrets: true
- id: execute-control
  provider: oci
  action: inspect
  profile: writable-root-filesystem
  arguments:
    target_ref: '{{ target.ref }}'
    control_id: DEVSEC-OCI-004
    rule_profile: writable-root-filesystem
    fail_closed: false
    redact_secrets: true
    collect_tool_version: true
    evidence_ref: findings/DEVSEC-OCI-004
- id: verify-evidence
  provider: evidence
  action: verify
  profile: deterministic-finding-bundle
  arguments:
    target_ref: '{{ target.ref }}'
    control_id: DEVSEC-OCI-004
    required_artifacts:
    - tool-version
    - scan-summary
    - sanitised-observations
    redact_secrets: true
evaluation:
  vulnerable_when:
  - control.writable_root_filesystem.status equals non_compliant
  - control.writable_root_filesystem.evidence_count is greater than zero
  - scanner.completed equals true and scanner.error_count equals zero
  secure_when:
  - control.writable_root_filesystem.status equals compliant
  - control.writable_root_filesystem.evidence_count equals zero
  - scanner.completed equals true and baseline.coverage_complete equals true
  inconclusive_when:
  - control.writable_root_filesystem.status equals unknown
  - scanner.completed equals false or scanner.error_count is greater than zero
  - baseline.coverage_complete equals false or required evidence is missing
outputs:
  title: Writable Root Filesystem control result
  severity: medium
  confidence: dynamic
