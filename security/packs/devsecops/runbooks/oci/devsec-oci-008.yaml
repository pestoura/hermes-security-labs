apiVersion: security.hex0r.io/v1alpha1
kind: SecurityRunbook
metadata:
  id: DEVSEC-OCI-008
  name: Verify Exposed Ports
  version: 1.0.0
  status: experimental
  domain: devsecops
  category: oci
  tags:
  - exposed-ports
  - oci
  - owasp-devsecops
  - typed-execution
selectors:
  target_types:
  - oci-image
  capabilities:
  - registry-read
  - image-metadata-read
risk:
  intrusiveness: passive
  destructive: false
  production_safe: true
  max_actions: 5
  timeout_seconds: 300
steps:
- id: collect-baseline
  provider: oci
  action: inspect
  profile: oci-baseline
  arguments:
    target_ref: '{{ target.ref }}'
    evidence_ref: baseline/DEVSEC-OCI-008
    read_only: true
    redact_secrets: true
- id: execute-control
  provider: oci
  action: inspect
  profile: exposed-ports
  arguments:
    target_ref: '{{ target.ref }}'
    control_id: DEVSEC-OCI-008
    rule_profile: exposed-ports
    fail_closed: false
    redact_secrets: true
    collect_tool_version: true
    evidence_ref: findings/DEVSEC-OCI-008
- id: verify-evidence
  provider: evidence
  action: verify
  profile: deterministic-finding-bundle
  arguments:
    target_ref: '{{ target.ref }}'
    control_id: DEVSEC-OCI-008
    required_artifacts:
    - tool-version
    - scan-summary
    - sanitised-observations
    redact_secrets: true
evaluation:
  vulnerable_when:
  - control.exposed_ports.status equals non_compliant
  - control.exposed_ports.evidence_count is greater than zero
  - scanner.completed equals true and scanner.error_count equals zero
  secure_when:
  - control.exposed_ports.status equals compliant
  - control.exposed_ports.evidence_count equals zero
  - scanner.completed equals true and baseline.coverage_complete equals true
  inconclusive_when:
  - control.exposed_ports.status equals unknown
  - scanner.completed equals false or scanner.error_count is greater than zero
  - baseline.coverage_complete equals false or required evidence is missing
outputs:
  title: Exposed Ports control result
  severity: medium
  confidence: dynamic
