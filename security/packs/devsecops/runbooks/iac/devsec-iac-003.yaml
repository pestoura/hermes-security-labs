apiVersion: security.hex0r.io/v1alpha1
kind: SecurityRunbook
metadata:
  id: DEVSEC-IAC-003
  name: Verify Public Database
  version: 1.0.0
  status: experimental
  domain: devsecops
  category: iac
  tags:
  - iac
  - owasp-devsecops
  - public-database
  - typed-execution
selectors:
  target_types:
  - iac-project
  capabilities:
  - iac-read
  - policy-engine
risk:
  intrusiveness: passive
  destructive: false
  production_safe: true
  max_actions: 5
  timeout_seconds: 300
steps:
- id: collect-baseline
  provider: repository
  action: inventory
  profile: iac-baseline
  arguments:
    target_ref: '{{ target.ref }}'
    evidence_ref: baseline/DEVSEC-IAC-003
    read_only: true
    redact_secrets: true
- id: execute-control
  provider: iac
  action: scan
  profile: public-database
  arguments:
    target_ref: '{{ target.ref }}'
    control_id: DEVSEC-IAC-003
    rule_profile: public-database
    fail_closed: false
    redact_secrets: true
    collect_tool_version: true
    evidence_ref: findings/DEVSEC-IAC-003
- id: verify-evidence
  provider: evidence
  action: verify
  profile: deterministic-finding-bundle
  arguments:
    target_ref: '{{ target.ref }}'
    control_id: DEVSEC-IAC-003
    required_artifacts:
    - tool-version
    - scan-summary
    - sanitised-observations
    redact_secrets: true
evaluation:
  vulnerable_when:
  - control.public_database.status equals non_compliant
  - control.public_database.evidence_count is greater than zero
  - scanner.completed equals true and scanner.error_count equals zero
  secure_when:
  - control.public_database.status equals compliant
  - control.public_database.evidence_count equals zero
  - scanner.completed equals true and baseline.coverage_complete equals true
  inconclusive_when:
  - control.public_database.status equals unknown
  - scanner.completed equals false or scanner.error_count is greater than zero
  - baseline.coverage_complete equals false or required evidence is missing
outputs:
  title: Public Database control result
  severity: high
  confidence: dynamic
