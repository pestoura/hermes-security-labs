apiVersion: security.hex0r.io/v1alpha1
kind: SecurityRunbook
metadata:
  id: DEVSEC-IAC-008
  name: Verify Hardcoded Secret
  version: 1.0.0
  status: experimental
  domain: devsecops
  category: iac
  tags:
  - hardcoded-secret
  - iac
  - owasp-devsecops
  - typed-execution
selectors:
  target_types:
  - iac-project
  capabilities:
  - iac-read
  - policy-engine
risk:
  intrusiveness: passive
  destructive: false
  production_safe: true
  max_actions: 5
  timeout_seconds: 300
steps:
- id: collect-baseline
  provider: repository
  action: inventory
  profile: iac-baseline
  arguments:
    target_ref: '{{ target.ref }}'
    evidence_ref: baseline/DEVSEC-IAC-008
    read_only: true
    redact_secrets: true
- id: execute-control
  provider: iac
  action: scan
  profile: hardcoded-secret
  arguments:
    target_ref: '{{ target.ref }}'
    control_id: DEVSEC-IAC-008
    rule_profile: hardcoded-secret
    fail_closed: false
    redact_secrets: true
    collect_tool_version: true
    evidence_ref: findings/DEVSEC-IAC-008
- id: verify-evidence
  provider: evidence
  action: verify
  profile: deterministic-finding-bundle
  arguments:
    target_ref: '{{ target.ref }}'
    control_id: DEVSEC-IAC-008
    required_artifacts:
    - tool-version
    - scan-summary
    - sanitised-observations
    redact_secrets: true
evaluation:
  vulnerable_when:
  - control.hardcoded_secret.status equals non_compliant
  - control.hardcoded_secret.evidence_count is greater than zero
  - scanner.completed equals true and scanner.error_count equals zero
  secure_when:
  - control.hardcoded_secret.status equals compliant
  - control.hardcoded_secret.evidence_count equals zero
  - scanner.completed equals true and baseline.coverage_complete equals true
  inconclusive_when:
  - control.hardcoded_secret.status equals unknown
  - scanner.completed equals false or scanner.error_count is greater than zero
  - baseline.coverage_complete equals false or required evidence is missing
outputs:
  title: Hardcoded Secret control result
  severity: medium
  confidence: dynamic
