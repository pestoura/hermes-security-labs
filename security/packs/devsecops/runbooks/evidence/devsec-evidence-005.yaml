apiVersion: security.hex0r.io/v1alpha1
kind: SecurityRunbook
metadata:
  id: DEVSEC-EVIDENCE-005
  name: Verify Tool Version Capture
  version: 1.0.0
  status: experimental
  domain: devsecops
  category: evidence
  tags:
  - evidence
  - owasp-devsecops
  - tool-version-capture
  - typed-execution
selectors:
  target_types:
  - evidence-bundle
  capabilities:
  - evidence-read
  - checksum-verify
risk:
  intrusiveness: low
  destructive: false
  production_safe: true
  max_actions: 5
  timeout_seconds: 180
steps:
- id: collect-baseline
  provider: evidence
  action: verify
  profile: evidence-baseline
  arguments:
    target_ref: '{{ target.ref }}'
    evidence_ref: baseline/DEVSEC-EVIDENCE-005
    read_only: true
    redact_secrets: true
- id: execute-control
  provider: evidence
  action: verify
  profile: tool-version-capture
  arguments:
    target_ref: '{{ target.ref }}'
    control_id: DEVSEC-EVIDENCE-005
    rule_profile: tool-version-capture
    fail_closed: false
    redact_secrets: true
    collect_tool_version: true
    evidence_ref: findings/DEVSEC-EVIDENCE-005
- id: verify-evidence
  provider: evidence
  action: verify
  profile: deterministic-finding-bundle
  arguments:
    target_ref: '{{ target.ref }}'
    control_id: DEVSEC-EVIDENCE-005
    required_artifacts:
    - tool-version
    - scan-summary
    - sanitised-observations
    redact_secrets: true
evaluation:
  vulnerable_when:
  - control.tool_version_capture.status equals non_compliant
  - control.tool_version_capture.evidence_count is greater than zero
  - scanner.completed equals true and scanner.error_count equals zero
  secure_when:
  - control.tool_version_capture.status equals compliant
  - control.tool_version_capture.evidence_count equals zero
  - scanner.completed equals true and baseline.coverage_complete equals true
  inconclusive_when:
  - control.tool_version_capture.status equals unknown
  - scanner.completed equals false or scanner.error_count is greater than zero
  - baseline.coverage_complete equals false or required evidence is missing
outputs:
  title: Tool Version Capture control result
  severity: medium
  confidence: dynamic
