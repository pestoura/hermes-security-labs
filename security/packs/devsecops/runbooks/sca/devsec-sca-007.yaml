apiVersion: security.hex0r.io/v1alpha1
kind: SecurityRunbook
metadata:
  id: DEVSEC-SCA-007
  name: Verify Lockfile Missing
  version: 1.0.0
  status: experimental
  domain: devsecops
  category: sca
  tags:
  - lockfile-missing
  - owasp-devsecops
  - sca
  - typed-execution
selectors:
  target_types:
  - software-project
  capabilities:
  - dependency-manifest-read
  - vulnerability-database
risk:
  intrusiveness: passive
  destructive: false
  production_safe: true
  max_actions: 5
  timeout_seconds: 300
steps:
- id: collect-baseline
  provider: repository
  action: inventory
  profile: sca-baseline
  arguments:
    target_ref: '{{ target.ref }}'
    evidence_ref: baseline/DEVSEC-SCA-007
    read_only: true
    redact_secrets: true
- id: execute-control
  provider: sca
  action: scan
  profile: lockfile-missing
  arguments:
    target_ref: '{{ target.ref }}'
    control_id: DEVSEC-SCA-007
    rule_profile: lockfile-missing
    fail_closed: false
    redact_secrets: true
    collect_tool_version: true
    evidence_ref: findings/DEVSEC-SCA-007
- id: verify-evidence
  provider: evidence
  action: verify
  profile: deterministic-finding-bundle
  arguments:
    target_ref: '{{ target.ref }}'
    control_id: DEVSEC-SCA-007
    required_artifacts:
    - tool-version
    - scan-summary
    - sanitised-observations
    redact_secrets: true
evaluation:
  vulnerable_when:
  - control.lockfile_missing.status equals non_compliant
  - control.lockfile_missing.evidence_count is greater than zero
  - scanner.completed equals true and scanner.error_count equals zero
  secure_when:
  - control.lockfile_missing.status equals compliant
  - control.lockfile_missing.evidence_count equals zero
  - scanner.completed equals true and baseline.coverage_complete equals true
  inconclusive_when:
  - control.lockfile_missing.status equals unknown
  - scanner.completed equals false or scanner.error_count is greater than zero
  - baseline.coverage_complete equals false or required evidence is missing
outputs:
  title: Lockfile Missing control result
  severity: high
  confidence: dynamic
