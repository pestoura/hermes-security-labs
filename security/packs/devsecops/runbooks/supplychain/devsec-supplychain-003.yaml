apiVersion: security.hex0r.io/v1alpha1
kind: SecurityRunbook
metadata:
  id: DEVSEC-SUPPLYCHAIN-003
  name: Verify Source Revision
  version: 1.0.0
  status: experimental
  domain: devsecops
  category: supplychain
  tags:
  - owasp-devsecops
  - source-revision
  - supplychain
  - typed-execution
selectors:
  target_types:
  - software-artifact
  capabilities:
  - attestation-read
  - signature-verify
risk:
  intrusiveness: passive
  destructive: false
  production_safe: true
  max_actions: 5
  timeout_seconds: 180
steps:
- id: collect-baseline
  provider: repository
  action: inventory
  profile: supplychain-baseline
  arguments:
    target_ref: '{{ target.ref }}'
    evidence_ref: baseline/DEVSEC-SUPPLYCHAIN-003
    read_only: true
    redact_secrets: true
- id: execute-control
  provider: supplychain
  action: verify
  profile: source-revision
  arguments:
    target_ref: '{{ target.ref }}'
    control_id: DEVSEC-SUPPLYCHAIN-003
    rule_profile: source-revision
    fail_closed: false
    redact_secrets: true
    collect_tool_version: true
    evidence_ref: findings/DEVSEC-SUPPLYCHAIN-003
- id: verify-evidence
  provider: evidence
  action: verify
  profile: deterministic-finding-bundle
  arguments:
    target_ref: '{{ target.ref }}'
    control_id: DEVSEC-SUPPLYCHAIN-003
    required_artifacts:
    - tool-version
    - scan-summary
    - sanitised-observations
    redact_secrets: true
evaluation:
  vulnerable_when:
  - control.source_revision.status equals non_compliant
  - control.source_revision.evidence_count is greater than zero
  - scanner.completed equals true and scanner.error_count equals zero
  secure_when:
  - control.source_revision.status equals compliant
  - control.source_revision.evidence_count equals zero
  - scanner.completed equals true and baseline.coverage_complete equals true
  inconclusive_when:
  - control.source_revision.status equals unknown
  - scanner.completed equals false or scanner.error_count is greater than zero
  - baseline.coverage_complete equals false or required evidence is missing
outputs:
  title: Source Revision control result
  severity: medium
  confidence: dynamic
