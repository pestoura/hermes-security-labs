apiVersion: security.hex0r.io/v1alpha1
kind: SecurityRunbook
metadata:
  id: DEVSEC-SUPPLYCHAIN-009
  name: Verify Hermetic Build
  version: 1.0.0
  status: experimental
  domain: devsecops
  category: supplychain
  tags:
  - hermetic-build
  - owasp-devsecops
  - supplychain
  - typed-execution
selectors:
  target_types:
  - software-artifact
  capabilities:
  - attestation-read
  - signature-verify
risk:
  intrusiveness: passive
  destructive: false
  production_safe: true
  max_actions: 5
  timeout_seconds: 180
steps:
- id: collect-baseline
  provider: repository
  action: inventory
  profile: supplychain-baseline
  arguments:
    target_ref: '{{ target.ref }}'
    evidence_ref: baseline/DEVSEC-SUPPLYCHAIN-009
    read_only: true
    redact_secrets: true
- id: execute-control
  provider: supplychain
  action: verify
  profile: hermetic-build
  arguments:
    target_ref: '{{ target.ref }}'
    control_id: DEVSEC-SUPPLYCHAIN-009
    rule_profile: hermetic-build
    fail_closed: false
    redact_secrets: true
    collect_tool_version: true
    evidence_ref: findings/DEVSEC-SUPPLYCHAIN-009
- id: verify-evidence
  provider: evidence
  action: verify
  profile: deterministic-finding-bundle
  arguments:
    target_ref: '{{ target.ref }}'
    control_id: DEVSEC-SUPPLYCHAIN-009
    required_artifacts:
    - tool-version
    - scan-summary
    - sanitised-observations
    redact_secrets: true
evaluation:
  vulnerable_when:
  - control.hermetic_build.status equals non_compliant
  - control.hermetic_build.evidence_count is greater than zero
  - scanner.completed equals true and scanner.error_count equals zero
  secure_when:
  - control.hermetic_build.status equals compliant
  - control.hermetic_build.evidence_count equals zero
  - scanner.completed equals true and baseline.coverage_complete equals true
  inconclusive_when:
  - control.hermetic_build.status equals unknown
  - scanner.completed equals false or scanner.error_count is greater than zero
  - baseline.coverage_complete equals false or required evidence is missing
outputs:
  title: Hermetic Build control result
  severity: medium
  confidence: dynamic
