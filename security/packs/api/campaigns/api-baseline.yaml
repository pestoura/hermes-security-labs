apiVersion: hex0r.io/v1alpha1
kind: ApiPentestCampaign
metadata:
  id: API-BASELINE-001
  name: API security baseline
selection:
  categories:
    - discovery
    - transport
    - authentication
    - authorization
    - data-exposure
    - configuration
execution:
  mode: sequential
  max_parallel: 1
  max_runbooks: 80
