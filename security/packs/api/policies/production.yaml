apiVersion: hex0r.io/v1alpha1
kind: ApiPentestPolicy
environment: production
scope:
  allowed_hosts: []
  allowed_cidrs: []
execution:
  allowed_intrusiveness: [passive, low]
  allow_destructive: false
  max_parallel: 1
  stop_on_scope_violation: true
