apiVersion: hex0r.io/v1alpha1
kind: ApiPentestRunbook
metadata:
  id: API-AUTHZ-VERTICAL-006
  name: Test vertical privilege escalation between roles
  version: 0.1.0
  status: experimental
  category: authorization
  tags:
  - api
  - authorization
  - workflow
  - vertical-privilege
mapping:
  owasp_api:
  - API5:2023
  cwe:
  - CWE-269
selectors:
  api_types:
  - rest
  auth_types:
  - api_key
  - bearer
  - cookie
  - jwt
  - oauth2
  - oidc
  required_capabilities:
  - roles
  - two_test_identities
risk:
  intrusiveness: medium
  production_safe: false
  destructive: false
  max_requests: 40
  max_response_bytes: 65536
inputs:
- name: base_url
  type: uri
  required: true
  secret: false
steps:
- id: primary-probe
  action: kali.action
  handler: workflow
  profile: vertical-privilege
  arguments:
    url: '{{ target.base_url }}'
  timeout_seconds: 120
  evidence:
  - request_metadata
  - response_status
  - response_headers
  - redacted_response_sample
  - timestamps
evaluation:
  vulnerable_when:
  - object_owner_id != subject_id and subject_id != '' and response_status == 200
  secure_when:
  - object_owner_id == subject_id or subject_id == ''
  inconclusive_when:
  - target_reachable == false or prerequisites_missing == true
outputs:
  title: Test vertical privilege escalation between roles
  severity: critical
  confidence: dynamic
