apiVersion: hex0r.io/v1alpha1
kind: ApiPentestRunbook
metadata:
  id: API-AUTHZ-RELATIONSHIP-022
  name: Test authorization on relationship endpoints
  version: 0.1.0
  status: experimental
  category: authorization
  tags:
  - api
  - authorization
  - workflow
  - relationship-authorization
mapping:
  owasp_api:
  - API1:2023
  cwe:
  - CWE-863
selectors:
  api_types:
  - rest
  auth_types:
  - api_key
  - bearer
  - cookie
  - jwt
  - oauth2
  - oidc
  required_capabilities:
  - two_test_identities
risk:
  intrusiveness: medium
  production_safe: false
  destructive: false
  max_requests: 40
  max_response_bytes: 65536
inputs:
- name: base_url
  type: uri
  required: true
  secret: false
steps:
- id: primary-probe
  action: kali.action
  handler: workflow
  profile: relationship-authorization
  arguments:
    url: '{{ target.base_url }}'
  timeout_seconds: 120
  evidence:
  - request_metadata
  - response_status
  - response_headers
  - redacted_response_sample
  - timestamps
evaluation:
  vulnerable_when:
  - workflow_status in {'failed','blocked'}
  secure_when:
  - workflow_status in {'passed','completed'}
  inconclusive_when:
  - target_reachable == false or prerequisites_missing == true
outputs:
  title: Test authorization on relationship endpoints
  severity: high
  confidence: dynamic
