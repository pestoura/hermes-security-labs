apiVersion: hex0r.io/v1alpha1
kind: ApiPentestRunbook
metadata:
  id: API-CONFIG-CORS-REFLECT-002
  name: Detect unsafe CORS origin reflection
  version: 0.1.0
  status: experimental
  category: configuration
  tags:
  - api
  - configuration
  - headers
  - cors-origin-reflection
mapping:
  owasp_api:
  - API8:2023
  cwe:
  - CWE-942
selectors:
  api_types:
  - rest
  auth_types:
  - any
  required_capabilities: []
risk:
  intrusiveness: low
  production_safe: true
  destructive: false
  max_requests: 20
  max_response_bytes: 65536
inputs:
- name: base_url
  type: uri
  required: true
  secret: false
steps:
- id: primary-probe
  action: kali.action
  handler: headers
  profile: cors-origin-reflection
  arguments:
    url: '{{ target.base_url }}'
  timeout_seconds: 60
  evidence:
  - request_metadata
  - response_status
  - response_headers
  - redacted_response_sample
  - timestamps
evaluation:
  vulnerable_when:
  - '''access-control-allow-origin'' in response_headers and ''access-control-allow-credentials''
    in response_headers'
  secure_when:
  - '''access-control-allow-origin'' not in response_headers or ''access-control-allow-credentials''
    not in response_headers'
  inconclusive_when:
  - target_reachable == false or prerequisites_missing == true
outputs:
  title: Detect unsafe CORS origin reflection
  severity: high
  confidence: dynamic
