apiVersion: hex0r.io/v1alpha1
kind: ApiPentestRunbook
metadata:
  id: API-AUTH-CREDENTIAL-RATE-019
  name: Validate credential attack rate limiting
  version: 0.1.0
  status: experimental
  category: authentication
  tags:
  - api
  - authentication
  - race
  - credential-rate-limit
mapping:
  owasp_api:
  - API2:2023
  cwe:
  - CWE-307
selectors:
  api_types:
  - rest
  auth_types:
  - any
  required_capabilities: []
risk:
  intrusiveness: high
  production_safe: false
  destructive: false
  max_requests: 100
  max_response_bytes: 65536
inputs:
- name: base_url
  type: uri
  required: true
  secret: false
steps:
- id: primary-probe
  action: kali.action
  handler: race
  profile: credential-rate-limit
  arguments:
    url: '{{ target.base_url }}'
  timeout_seconds: 180
  evidence:
  - request_metadata
  - response_status
  - response_headers
  - redacted_response_sample
  - timestamps
evaluation:
  vulnerable_when:
  - race_duplicate_success==True
  secure_when:
  - race_duplicate_success==False
  inconclusive_when:
  - target_reachable == false or prerequisites_missing == true
outputs:
  title: Validate credential attack rate limiting
  severity: high
  confidence: dynamic
