apiVersion: hex0r.io/v1alpha1
kind: ApiPentestRunbook
metadata:
  id: API-TRANS-CERT-EXPIRY-005
  name: Validate certificate expiry and validity window
  version: 0.1.0
  status: experimental
  category: transport
  tags:
  - api
  - transport
  - tls
  - certificate-expiry
mapping:
  owasp_api:
  - API8:2023
  cwe:
  - CWE-298
selectors:
  api_types:
  - rest
  auth_types:
  - any
  required_capabilities: []
risk:
  intrusiveness: passive
  production_safe: true
  destructive: false
  max_requests: 10
  max_response_bytes: 65536
inputs:
- name: base_url
  type: uri
  required: true
  secret: false
steps:
- id: primary-probe
  action: kali.action
  handler: tls
  profile: certificate-expiry
  arguments:
    url: '{{ target.base_url }}'
  timeout_seconds: 60
  evidence:
  - request_metadata
  - response_status
  - response_headers
  - redacted_response_sample
  - timestamps
evaluation:
  vulnerable_when:
  - ssl_grade in {'F','T'} or 'heartbleed' in nmap_script_output
  secure_when:
  - ssl_grade in {'A+','A','B'} and 'heartbleed' not in nmap_script_output
  inconclusive_when:
  - target_reachable == false or prerequisites_missing == true
outputs:
  title: Validate certificate expiry and validity window
  severity: medium
  confidence: dynamic
