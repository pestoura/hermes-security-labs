apiVersion: hex0r.io/v1alpha1
kind: ApiPentestRunbook
metadata:
  id: API-TOKEN-COOKIE-SAMESITE-003
  name: Validate SameSite controls on authentication cookies
  version: 0.1.0
  status: experimental
  category: token-session
  tags:
  - api
  - token-session
  - headers
  - cookie-samesite
mapping:
  owasp_api:
  - API2:2023
  cwe:
  - CWE-1275
selectors:
  api_types:
  - rest
  auth_types:
  - any
  required_capabilities: []
risk:
  intrusiveness: medium
  production_safe: false
  destructive: false
  max_requests: 20
  max_response_bytes: 65536
inputs:
- name: base_url
  type: uri
  required: true
  secret: false
steps:
- id: primary-probe
  action: kali.action
  handler: headers
  profile: cookie-samesite
  arguments:
    url: '{{ target.base_url }}'
  timeout_seconds: 60
  evidence:
  - request_metadata
  - response_status
  - response_headers
  - redacted_response_sample
  - timestamps
evaluation:
  vulnerable_when:
  - response_headers is not None and len(response_headers) == 0 or handler_signal
    in {'insecure','vulnerable','failed'}
  secure_when:
  - response_headers is not None and len(response_headers) > 0 or handler_signal in
    {'secure','hardened','passed'}
  inconclusive_when:
  - target_reachable == false or prerequisites_missing == true
outputs:
  title: Validate SameSite controls on authentication cookies
  severity: medium
  confidence: dynamic
