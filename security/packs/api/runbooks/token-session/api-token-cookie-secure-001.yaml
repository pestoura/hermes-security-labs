apiVersion: hex0r.io/v1alpha1
kind: ApiPentestRunbook
metadata:
  id: API-TOKEN-COOKIE-SECURE-001
  name: Validate Secure flag on authentication cookies
  version: 0.1.0
  status: experimental
  category: token-session
  tags:
  - api
  - token-session
  - headers
  - cookie-secure
mapping:
  owasp_api:
  - API2:2023
  cwe:
  - CWE-614
selectors:
  api_types:
  - rest
  auth_types:
  - any
  required_capabilities: []
risk:
  intrusiveness: medium
  production_safe: false
  destructive: false
  max_requests: 20
  max_response_bytes: 65536
inputs:
- name: base_url
  type: uri
  required: true
  secret: false
steps:
- id: primary-probe
  action: kali.action
  handler: headers
  profile: cookie-secure
  arguments:
    url: '{{ target.base_url }}'
  timeout_seconds: 60
  evidence:
  - request_metadata
  - response_status
  - response_headers
  - redacted_response_sample
  - timestamps
evaluation:
  vulnerable_when:
  - '''x-content-type-options'' not in response_headers or ''x-frame-options'' not
    in response_headers'
  secure_when:
  - '''x-content-type-options'' in response_headers and ''x-frame-options'' in response_headers'
  inconclusive_when:
  - target_reachable == false or prerequisites_missing == true
outputs:
  title: Validate Secure flag on authentication cookies
  severity: medium
  confidence: dynamic
